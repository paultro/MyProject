  本系统使用以结构体为节点的链表实现了基本的对学生信息，课程信息的增删改查。
  在MyProject的结构中，main目录里有全部全部代码的main.c，和可执行文件main，是第一次写的时候写在一块以便查看更改语法错误和逻辑错误。
  其余为拆分成各个模块之后的整个工程。main1是由runmain.sh生成的可执行文件，main2是由多级makefile最终生成的可执行文件，均可正常使用。
  include目录里放的是各个子模块的头文件以及通用的结构体链表的头文件。
  maintest目录里放的是拆分后的main函数。
  src里放的是各个实现各自功能的子模块。
  obj里放的是各个子模块的.o文件以及它们生成的静态库文件。
  我没有做关联，因为我觉得与其定义诸多类似#之类的符号以实现2#3=2*3+3/3,不如学会加减乘除，方法是死的人是活的，只要我为用户提供的方法可以组合实现用户需求，那这个系统起码是可以用的。
